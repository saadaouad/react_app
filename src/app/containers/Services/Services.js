import React, { Component } from 'react';
import Helmet from 'react-helmet';

class Services extends Component {
    render() {
        const style = require('./Services.scss');
        return (
            <div className='services container'>
            </div>
        )
    }
}

export default Services;
